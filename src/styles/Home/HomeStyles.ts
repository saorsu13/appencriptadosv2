// src/styles/HomeStyles.ts
import { StyleSheet } from 'react-native';

export const HomeStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
  scrollContent: {
    padding: 16,
  },
  // Aquí irán tus estilos de contenedores, márgenes, etc.
});
